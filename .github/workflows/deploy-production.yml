name: Deploy Production

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder Production Deploy
        run: echo "TODO: Implement ECR push and ECS deploy for production"
